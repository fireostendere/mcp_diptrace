services:
  qdrant:
    image: qdrant/qdrant:v1.19.0
    container_name: kb-qdrant
    ports:
      - "6333:6333"   # HTTP (client)
      - "6334:6334"   # gRPC (unused by default, handy for debugging)
    volumes:
      - ./data/qdrant_storage:/qdrant/storage
    restart: unless-stopped
