# Architecture cleanup notes

The refactor is designed to be behavior-preserving. Public MCP tool names, input/output schemas, service Facade signatures, safety boundaries, and release artifact policy remain guarded by existing snapshots and CI checks.
