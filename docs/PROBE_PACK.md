<!-- Generated by scripts/make_probe_pack.py from docs/OPEN_QUESTIONS.md. Do not edit by hand. -->

# DipTrace Operator Probe Pack

This generated pack turns each maintained open question into an operator-facing procedure. It reproduces project-authored question and experiment text; it does not answer the question, authenticate an export, grant provenance, or promote anything into `tests/fixtures/acceptance/`.

It currently contains **18 open probes**. Edit [OPEN_QUESTIONS.md](OPEN_QUESTIONS.md), then regenerate this file.

## Common capture workflow

Before opening DipTrace:

1. Read the complete question and perform the preparation stated in its operator recipe on a disposable design copy.
2. Put the concrete recipe and every input/export under one private allowed root. Do not use the repository or acceptance fixture tree as that root.
3. Run [`scripts/capture_diptrace_evidence.py`](../scripts/capture_diptrace_evidence.py) in the order documented by [Operator-assisted DipTrace evidence capture](EVIDENCE_CAPTURE.md): `init`, `record source`, `record open_save`, `record reexport`, every required `check`, then `finalize`.
4. Treat the resulting manifest only as an untrusted review candidate. Preserve literal bytes and report discrepancies; never repair an export or infer the unknown answer.

The exact CLI flags, attestation templates, containment rules, resume behavior, and review boundary are maintained in [EVIDENCE_CAPTURE.md](EVIDENCE_CAPTURE.md#interactive-run).

---

## Q1 — Is `Component/@Angle` in radians or degrees?

[Maintained source question](OPEN_QUESTIONS.md#q1-is-componentangle-in-radians-or-degrees)

### Unknown to test

The project-manual Q1 gate is PASS on DipTrace PCB Layout 5.3.0.3: 90/180/270 degrees serialized as approximately 1.5708/3.1416/4.7124 radians, and the bottom-side change-side case was observed. What remains open is a redistributable/package-owned capture suitable for promoting that convention into reusable public trust evidence.

### Why the implementation depends on it

`src/diptrace_mcp/adapters.py::_component_records` converts stored radians to degrees and `src/diptrace_mcp/semantic_compiler.py::_set_angle_attribute` writes radians. The implementation convention is now supported by manual host evidence, but release/public evidence must remain scoped.

### Documented boundary

`docs/MANUAL_ACCEPTANCE_CHECKPOINT_2026-08-09.md` and `docs/XML_COMPATIBILITY.md` record the later manual PASS. Immutable `v0.2.1` release evidence correctly remains `NOT_RUN` because it predates that campaign.

### Operator recipe

Repeat the controlled 90/180/270 degree and change-side probe with redistributable artifacts using the committed [Q1 capture recipe](evidence_capture/q1-component-angle.recipe.json), preserving distinct source/open-save/re-export roles and literal angle values.

**Operator required:** Human with licensed DipTrace PCB Layout 5.3.x and permission to provide sanitized evidence.

**Capture handoff:**

Use the committed [Q1 concrete recipe](evidence_capture/q1-component-angle.recipe.json). Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q2 — Which objects can an `ImpMode=All` apply delete because the exchange export omitted them?

[Maintained source question](OPEN_QUESTIONS.md#q2-which-objects-can-an-impmodeall-apply-delete-because-the-exchange-export-omitted-them)

### Unknown to test

Is an `ExpMode=All` exchange complete enough that returning it under `ImpMode=All` cannot delete design objects or embedded records omitted from the exchange?

### Why the implementation depends on it

`src/diptrace_mcp/sessions.py::SessionStore.finalize` returns the complete working exchange file on apply, so object classes omitted by DipTrace export can matter even when MCP changes only one scalar.

### Documented boundary

A prior live round trip observed DipTrace canonicalization/removal of unreferenced embedded records, but the project has not enumerated every object class that `ExpMode=All` may omit.

### Operator recipe

On a disposable object-rich board, compare source/open-save/re-export counts, IDs and relevant subtree hashes before and after one unrelated scalar edit plus an unchanged control.

**Operator required:** Human with DipTrace 5.3 and a disposable representative design.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q3 — Does Cancel prevent an `ImpMode=All` exchange from being re-imported?

[Maintained source question](OPEN_QUESTIONS.md#q3-does-cancel-prevent-an-impmodeall-exchange-from-being-re-imported)

### Unknown to test

The 2026-07-31 DipTrace 5.2.0.4 live campaign proved PCB and Schematic Cancel preserved the tested host state. Does the same behavior hold for the current DipTrace 5.3/profile combinations and broader object sets used by future compatibility claims?

### Why the implementation depends on it

`src/diptrace_mcp/bridge.py::BridgeController.finish` distinguishes apply/cancel locally, but the project should not generalize one exact-version host observation to every later editor/profile without evidence.

### Documented boundary

`docs/LIVE_ACCEPTANCE_2026-07-31.md` records PCB/Schematic Cancel PASS for DipTrace 5.2.0.4. Current docs treat that as exact-scope evidence, not a universal guarantee.

### Operator recipe

Repeat Cancel on disposable current-version PCB and Schematic designs after a recognizable harmless working-copy edit, then save/re-export and verify the edit never reached host state.

**Operator required:** Human with the current DipTrace 5.3 configuration used for the target compatibility claim.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q4 — Does DipTrace address top-level list entries by `Id` or by position?

[Maintained source question](OPEN_QUESTIONS.md#q4-does-diptrace-address-top-level-list-entries-by-id-or-by-position)

### Unknown to test

Are sparse IDs and middle-list removals preserved, or does DipTrace renumber entries by list position?

### Why the implementation depends on it

`src/diptrace_mcp/semantic_compiler.py::_apply_sync_schematic_to_pcb` can remove cross-referenced objects, so host renumbering behavior matters to exact synchronization safety.

### Operator recipe

In a disposable design remove a middle object, then repeat with deliberately sparse IDs; save/re-export and compare every remaining ID/reference.

**Operator required:** Human with DipTrace and a disposable cross-referenced design.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q5 — Which routed `Point` attributes does DipTrace 5.3 require and preserve?

[Maintained source question](OPEN_QUESTIONS.md#q5-which-routed-point-attributes-does-diptrace-53-require-and-preserve)

### Unknown to test

Which optional `Point` attributes are emitted, required, derived or preserved for straight segments, arcs, jumpers, vias, necks, meanders and differential-pair points?

### Why the implementation depends on it

`src/diptrace_mcp/routing_compiler.py::_write_points` owns a bounded known attribute set, so an unmodeled condition-dependent attribute could otherwise be lost when a trace is replaced.

### Operator recipe

Export minimal examples of each geometry, compare per-Point attributes, then remove one optional-looking attribute at a time in disposable imports and re-export.

**Operator required:** Human with DipTrace 5.3 who can create each routing geometry.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q6 — Under object setting `All`, does `Selected="Y"` become persistent imported selection state?

[Maintained source question](OPEN_QUESTIONS.md#q6-under-object-setting-all-does-selectedy-become-persistent-imported-selection-state)

### Unknown to test

For a new PCB or schematic object imported under selector `All`, does `Selected="Y"` control or persist post-import UI selection state?

### Why the implementation depends on it

`src/diptrace_mcp/semantic_compiler.py::_apply_add_testpoint` and other creation paths intentionally emit unselected objects, so persistent semantics should not be guessed.

### Operator recipe

Import otherwise identical objects with `Selected="Y"` and `Selected="N"`, inspect immediate UI state, then save/re-export both.

**Operator required:** Human with a licensed DipTrace installation.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q7 — Which `Real` spellings does DipTrace emit and accept?

[Maintained source question](OPEN_QUESTIONS.md#q7-which-real-spellings-does-diptrace-emit-and-accept)

### Unknown to test

What precision/lexical forms does DipTrace emit for real numbers, and does it accept equivalent scientific notation?

### Why the implementation depends on it

`src/diptrace_mcp/semantic_compiler.py::_set_angle_attribute` and other writers use bounded numeric formatting that may differ lexically from DipTrace canonical output.

### Operator recipe

Export a range of magnitudes/precisions, then change one disposable valid decimal to equivalent scientific notation and observe import/save/re-export behavior.

**Operator required:** Human with a licensed DipTrace installation.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q8 — Which encoding, BOM, and line endings does DipTrace export?

[Maintained source question](OPEN_QUESTIONS.md#q8-which-encoding-bom-and-line-endings-does-diptrace-export)

### Unknown to test

What encoding, BOM policy, XML declaration spelling and line endings does each DipTrace editor emit?

### Why the implementation depends on it

`src/diptrace_mcp/xml_document.py::DipTraceDocument.from_bytes` preserves several supported source encodings/BOMs, but parser support does not establish host export policy.

### Operator recipe

Export minimal PCB/Schematic/Component/Pattern documents with non-ASCII text and inspect raw bytes for encoding, BOM, declaration and LF/CRLF.

**Operator required:** Human with each relevant DipTrace 5.3 editor.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q9 — Must an MCP edit preserve the source encoding and BOM?

[Maintained source question](OPEN_QUESTIONS.md#q9-must-an-mcp-edit-preserve-the-source-encoding-and-bom)

### Unknown to test

Which loaded encodings can be returned after an edit without host rejection/corruption, and must the original BOM/declaration be preserved?

### Why the implementation depends on it

`src/diptrace_mcp/xml_document.py::DipTraceDocument.apply_edits` preserves supported source codecs/BOMs as a safety policy, but that does not prove DipTrace accepts every parser-supported encoding.

### Operator recipe

Apply one raw edit and one structural insertion to controlled UTF-8/BOM/UTF-16 cases that DipTrace can open, then save/re-export and verify text plus declaration/BOM.

**Operator required:** Human with DipTrace and provenance-preserving encoded fixtures.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q10 — How does DipTrace 5.3 encode authoritative copper-pour fill data?

[Maintained source question](OPEN_QUESTIONS.md#q10-how-does-diptrace-53-encode-authoritative-copper-pour-fill-data)

### Unknown to test

Where and how does DipTrace represent authoritative refilled copper, thermals, cutouts and islands in XML/exchange data?

### Why the implementation depends on it

`src/diptrace_mcp/adapters.py::_board_copper_pour_records` models the observed boundary but does not promote it to authoritative final refill geometry.

### Operator recipe

Create a controlled pour with thermals/cutout/island/obstacle, export before and after refill, and compare every CopperPour child/payload without assuming a decoder.

**Operator required:** Human with DipTrace 5.3 and a controlled pour fixture.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q11 — What are the standalone Component and Pattern Editor XML mutation semantics?

[Maintained source question](OPEN_QUESTIONS.md#q11-what-are-the-standalone-component-and-pattern-editor-xml-mutation-semantics)

### Unknown to test

The internal raw-preserving mutation core now has controlled real Component Editor and Pattern Editor save/reopen/re-export evidence. What remains open is the complete identity/canonicalization space, including `UID32`, partial-vs-full export behavior, and the exact subset safe enough for any future public native-library write contract.

### Why the implementation depends on it

`src/diptrace_mcp/library_mutation.py::LibraryMutationEngine` must preserve unowned structures and stable relationships, while public API design must not generalize beyond the operations/editor cases actually verified.

### Documented boundary

Current `main` no longer claims that native library mutation is unimplemented. The core is internal/evidence-scoped and public native-library mutation remains unregistered.

### Operator recipe

On disposable minimal Component and Pattern libraries, compare repeated untouched exports, full-vs-current-object export, controlled one-setting mutations, UID32/reference stability, save/reopen/re-export and idempotence.

**Operator required:** Human with licensed Component/Pattern Editors and permission to share sanitized evidence.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q12 — How does DipTrace acknowledge plug-in failure or output corruption?

[Maintained source question](OPEN_QUESTIONS.md#q12-how-does-diptrace-acknowledge-plug-in-failure-or-output-corruption)

### Unknown to test

What does DipTrace do when a plug-in exits non-zero, exits zero unchanged, returns malformed/truncated XML, or returns one valid edit?

### Why the implementation depends on it

`src/diptrace_mcp/bridge.py::BridgeController.finish` reports local finalization but receives no typed host acknowledgement proving import success.

### Operator recipe

On disposable files exercise the four controlled outcomes, record host dialogs/state/exit behavior, and independently re-export after each.

**Operator required:** Human with DipTrace and a controlled test plug-in.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q13 — Which DipTrace 5.3 XML elements and attributes are absent from current observations?

[Maintained source question](OPEN_QUESTIONS.md#q13-which-diptrace-53-xml-elements-and-attributes-are-absent-from-current-observations)

### Unknown to test

What vocabulary/semantics does representative DipTrace 5.3 data emit that is absent from the project-owned observation inventory?

### Why the implementation depends on it

`src/diptrace_mcp/adapters.py::build_snapshot` normalizes known structures while unknown raw XML may remain invisible to analysis or at risk when an owning parent is regenerated.

### Operator recipe

Compare representative controlled 5.3 exports against `reference/diptrace-xml/spec_inventory.json`, recording unknown element paths/attributes without promoting synthetic observations.

**Operator required:** Human with DipTrace 5.3 and suitable representative designs.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q14 — Can DipTrace 5.3 open and save its native XML project format directly?

[Maintained source question](OPEN_QUESTIONS.md#q14-can-diptrace-53-open-and-save-its-native-xml-project-format-directly)

### Unknown to test

Can current XML `.dip`, `.dch`, `.eli` or `.lib` files be opened/saved directly without the plug-in exchange lifecycle, and how does that path differ from exchange XML?

### Why the implementation depends on it

`src/diptrace_mcp/xml_document.py::DipTraceDocument.from_bytes` accepts supported XML roots regardless of extension, while live writes still use the bridge/session trust model.

### Operator recipe

On a disposable native XML copy make one safe scalar change, open/save directly in DipTrace, re-export, and compare with an exchange export from the same design.

**Operator required:** Human with DipTrace 5.3 using disposable project copies.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q15 — What does DipTrace canonicalize on open, save, and re-export?

[Maintained source question](OPEN_QUESTIONS.md#q15-what-does-diptrace-canonicalize-on-open-save-and-re-export)

### Unknown to test

Which byte and semantic changes does DipTrace itself make to an otherwise untouched export/native XML project?

### Why the implementation depends on it

`src/diptrace_mcp/xml_document.py::RawTreeSnapshot.compile` protects MCP-owned byte regions while host canonicalization may independently change ordering/defaults/numbers/derived records.

### Operator recipe

Export, open/save without editing, re-export, and compare byte-level plus semantic changes separately.

**Operator required:** Human with DipTrace and a representative disposable design.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q16 — How does the same design scale across root `Units=mm`, `inch`, and `mil`?

[Maintained source question](OPEN_QUESTIONS.md#q16-how-does-the-same-design-scale-across-root-unitsmm-inch-and-mil)

### Unknown to test

Does DipTrace rescale every dimensional field consistently across root units, or are some fields stored in a fixed unit?

### Why the implementation depends on it

`src/diptrace_mcp/geometry.py::to_mm` and `src/diptrace_mcp/geometry.py::from_mm` assume dimensional fields follow root units unless explicitly modeled otherwise.

### Operator recipe

Export the same unchanged design in mm/inch/mil and compare normalized dimensions field by field, recording exceptions rather than inferring them.

**Operator required:** Human with DipTrace and control of export units.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q17 — What DSN/SES conventions does DipTrace use on a real routed design?

[Maintained source question](OPEN_QUESTIONS.md#q17-what-dsnses-conventions-does-diptrace-use-on-a-real-routed-design)

### Unknown to test

Which Specctra quoting, layer, padstack, via, coordinate and session constructs does DipTrace emit/accept in a real DSN/SES round trip?

### Why the implementation depends on it

`src/diptrace_mcp/specctra.py::export_dsn` and `src/diptrace_mcp/specctra.py::parse_ses` have bounded regression coverage but no committed real paired DipTrace DSN/SES corpus proving all host conventions.

### Operator recipe

Export a controlled multilayer DSN, route a small change in a compatible router, import SES, then preserve/compare original DSN/SES/source/final board XML.

**Operator required:** Human with DipTrace and a compatible Specctra-format router.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.

---

## Q18 — What hierarchy records are required for a real multi-sheet schematic?

[Maintained source question](OPEN_QUESTIONS.md#q18-what-hierarchy-records-are-required-for-a-real-multi-sheet-schematic)

### Unknown to test

Which IDs, paths, connectors and net records must agree for DipTrace to preserve a hierarchical multi-sheet schematic?

### Why the implementation depends on it

`src/diptrace_mcp/adapters.py::_schematic_sheets` reads known hierarchy data while `src/diptrace_mcp/semantic_compiler.py::_apply_add_sheet` and `_apply_add_wire` author only the currently implemented subset.

### Operator recipe

Build a minimal two-level hierarchy with repeated instances/local/global nets/connectors, then export/open-save/re-export and compare paths, IDs, endpoints and logical net membership.

**Operator required:** Human with DipTrace and a controlled hierarchical schematic.

**Capture handoff:**

Create a question-specific `diptrace-capture-recipe-v1` by copying the [strict recipe template](evidence_capture/pcb-format-question.recipe.template.json). Replace its generic feature and checklist text only with observations required by the experiment above; do not encode an expected result. Then follow the [interactive capture sequence](EVIDENCE_CAPTURE.md#interactive-run) with distinct `source`, `open_save`, and `reexport` XML files.

The collector applies only when this probe yields all three valid XML roles. Never substitute a screenshot, log, malformed file, or repeated path for a missing role. If the experiment cannot produce the triple, stop the capture and keep the question open; extending the evidence schema is a separate reviewed code change.
