# Architecture cleanup status

Implementation branch: `agent/architecture-debt-cleanup`.

The branch is intentionally kept as one integration line from `main`. Intermediate bootstrap files are one-shot and are removed by the refactor workflow before the final implementation commit. The final acceptance signal is the complete pull-request GitHub Actions suite.
